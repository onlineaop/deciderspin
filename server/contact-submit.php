<?php
/**
 * Superseded — this raw PHP file can't actually be deployed: Novamira only
 * allows PHP writes inside its WordPress sandbox directory
 * (wp-content/novamira-sandbox/), not at the public document root. The
 * real contact form handler is a WordPress REST route registered from
 * wp-content/novamira-sandbox/deciderspin-contact.php on the server,
 * posted to at /wp-json/deciderspin/v1/contact (see components/ContactForm.tsx).
 *
 * Left as an inert placeholder rather than deleted.
 */
