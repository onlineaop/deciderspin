# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# legacy pre-Next.js static prototype (superseded by app/ — kept locally
# for reference only, never part of this repo)
/wheel/
/8ball/

# deployment artifacts (static export output + the zip/tar built from it
# to upload to the server) — regenerate with `npm run build` + make-zip.ps1,
# never part of the repo itself
/out/
/deciderspin-export.zip
/deciderspin-export.tar
/make-zip.ps1

