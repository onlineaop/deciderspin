// Superseded by server/contact-submit.php — a Next.js static export (see
// next.config.ts, output: "export") has no server to run Server Actions
// against, so the contact form now posts directly to that PHP script
// instead. Left as an empty placeholder rather than deleted.
export {};
